apiVersion: 1

providers:
  - name: suilens-observability
    folder: SuiLens
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
